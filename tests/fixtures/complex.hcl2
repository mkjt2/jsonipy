title = "Complex HCL2 Example"
tags = ["config", "test", "example"]

server {
  enabled = true
  ports = [8080, 8081, 8082]

  auth {
    username = "admin"
    timeout = 30
  }
}

services {
  name = "web"
  priority = 1
}

services {
  name = "api"
  priority = 2
}
